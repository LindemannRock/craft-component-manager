{% extends "_layouts/cp" %}

{% set title = title ?? 'Settings'|t('twig-component-manager') %}
{% set selectedSettingsItem = selectedSettingsItem ?? 'general' %}
{% set selectedSubnavItem = 'settings' %}

{% block sidebar %}
    <nav id="settings-nav">
        <ul>
            <li>
                <a href="{{ url('twig-component-manager/settings/general') }}" {% if selectedSettingsItem == 'general' %}class="sel"{% endif %} data-nav-item="general">
                    {{ 'General'|t('twig-component-manager') }}
                </a>
            </li>
            <li>
                <a href="{{ url('twig-component-manager/settings/paths') }}" {% if selectedSettingsItem == 'paths' %}class="sel"{% endif %} data-nav-item="paths">
                    {{ 'Paths & Files'|t('twig-component-manager') }}
                </a>
            </li>
            <li>
                <a href="{{ url('twig-component-manager/settings/features') }}" {% if selectedSettingsItem == 'features' %}class="sel"{% endif %} data-nav-item="features">
                    {{ 'Features'|t('twig-component-manager') }}
                </a>
            </li>
            <li>
                <a href="{{ url('twig-component-manager/settings/discovery') }}" {% if selectedSettingsItem == 'discovery' %}class="sel"{% endif %} data-nav-item="discovery">
                    {{ 'Discovery'|t('twig-component-manager') }}
                </a>
            </li>
            <li>
                <a href="{{ url('twig-component-manager/settings/library') }}" {% if selectedSettingsItem == 'library' %}class="sel"{% endif %} data-nav-item="library">
                    {{ 'Component Library'|t('twig-component-manager') }}
                </a>
            </li>
            <li>
                <a href="{{ url('twig-component-manager/settings/maintenance') }}" {% if selectedSettingsItem == 'maintenance' %}class="sel"{% endif %} data-nav-item="maintenance">
                    {{ 'Maintenance'|t('twig-component-manager') }}
                </a>
            </li>
        </ul>
    </nav>
{% endblock %}

{% css %}
    /* Prevent scroll-based highlighting */
    #settings-nav a {
        transition: none;
    }
    
    /* Ensure only the current page link is highlighted */
    #settings-nav a.sel {
        cursor: default;
    }
{% endcss %}